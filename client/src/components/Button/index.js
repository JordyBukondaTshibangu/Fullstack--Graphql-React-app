import React from 'react'
import { ButtonComponent } from './ButtonElement'

const Button = ({text}) => {
    return (
        <ButtonComponent>
            { text }
        </ButtonComponent>
    )
}

export default Button
